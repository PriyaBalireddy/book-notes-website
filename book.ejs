<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= book.title %> - Book Details</title>
  <link rel="stylesheet" href="/css/book.css">
</head>
<body>

<div class="container">
  <h1>&#128214 <%= book.title %></h1>
  <h3>by <%= book.author %></h3>

  <div class="book-details">
    <img src="<%= book.cover_url %>" alt="Book Cover" width="200">
    <p><strong>Rating:</strong> &#11088 <%= book.rating %>/5</p>
    <p><strong>Added By:</strong> <%= book.user %></p>
  </div>

  <% if (book.user === session.user) { %>
    <a href="/edit/<%= book.id %>">
      <button>Edit Book</button>
    </a>

    <form action="/delete/<%= book.id %>" method="post" style="display:inline;">
      <button type="submit" onclick="return confirm('Are you sure you want to delete this book?')">
        U+274C Delete Book
      </button>
    </form>
  <% } %>

  <br><br>
  <a href="/">&#x2B05 Back to Home</a>
</div>
</body>
</html>
